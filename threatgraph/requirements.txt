# Core engine (Modules 1, 2, 4) — required
networkx>=3.0

# API + visualization backend (Module 3) — required to run the server
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Production graph backend (Module 2) — optional, activated by NEO4J_URI
neo4j>=5.0

# Optional integrations (activated only when the matching env var is set)
# kafka-python>=2.0      # Module 1 streaming ingestion (KAFKA_BROKER)
# pycti>=6.0             # Module 4 OpenCTI enrichment (OPENCTI_URL/TOKEN)

# Tests
pytest>=7.0
pytest-asyncio>=0.23
httpx>=0.27            # async test client for FastAPI (replaces requests in tests)
 